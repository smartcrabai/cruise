When this formal-specification mode is enabled, keep the normal Markdown implementation plan as the primary document. Do not replace its requirements, design, impact analysis, implementation steps, or test strategy with formal notation.

In addition to that Markdown plan, express every requirement that can be represented in a formal model in both a fenced `quint` block and a fenced `alloy` block. The output must contain a Markdown opening fence of ```quint for the Quint model and another opening fence of ```alloy for the Alloy model. Omit a notation only when the requirement truly cannot be represented in that notation, and state the reason in the surrounding Markdown. Do not invent pseudo-syntax: use valid, executable-looking Quint syntax and valid Alloy syntax for the constructs you write.

For each model, preserve the exact meaning of the Markdown requirements rather than weakening a condition or result. Model the relevant state and initial state, transitions, temporal requirements, invariants, ownership relationships, and cardinality constraints as applicable. Include the transition predicates that temporal properties refer to, and include a trace constraint in Alloy so the modeled transitions and temporal requirements are part of the same model.

Before every requirement-level formal structure, write a standalone natural-language comment in `{plan.language}` explaining the domain meaning of that structure. Comments must describe the condition and result well enough to reconstruct the requirement, but comments never replace the formal expression. In the Quint model, use the correct definition mode and state initialization, and account for stuttering and fairness when stating liveness. In the Alloy model, keep signatures, facts, predicates, and assertions internally consistent.

Check before submitting the plan that both Quint and Alloy blocks are present, that every formal expression uses valid syntax, that each transition preserves the stated invariants, that the requested final state is reachable from the initial state, and that the semantic comments and formal expressions agree. Mention any genuine representation limitation explicitly in Markdown.
