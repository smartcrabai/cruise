# Incomplete Handoff: cleanup_after_pr Feature

## Status
Core Rust compilation fixes applied; `cargo test --workspace` passes. Integration tests for cleanup behavior and documentation/GUI updates remain.

## Done
- Fixed `args.session` partial-move bug in `src/run_cmd.rs` by computing `cleanup_after_pr_override()` before the move.
- Added missing `cleanup_after_pr` / `no_cleanup_after_pr` fields to all direct `RunArgs { ... }` constructions:
  - `src/exec_cmd.rs`
  - `src/list_cmd.rs`
  - `src/plan_cmd.rs`
  - `src/run_cmd.rs` (5 test constructions + `run_all` propagation was already present)
- Verified `cargo test --workspace` passes (891 cruise + 127 cruise_gui tests).

## Remaining
1. **Add integration tests in `src/run_cmd.rs`** for:
   - `test_run_cleans_up_worktree_when_enabled`
   - `test_run_keeps_worktree_when_disabled`
   - `test_run_keeps_worktree_on_failure`
   - `test_run_keeps_worktree_for_repo_session`
   - `test_cli_flag_overrides_config_to_true`
   - `test_cli_no_flag_overrides_config_to_false`
   Use the existing test fixtures (`create_repo_with_origin`, `install_logging_gh`, `run_args`, etc.).
2. **Update `src-tauri/src/commands.rs`**:
   - Add equivalent cleanup logic after `handle_worktree_pr` in GUI run paths.
   - Add `cleanup_after_pr: Option<bool>` argument to new-session creation commands and store it in `SessionState.cleanup_after_pr_override`.
   - Add `cleanup_after_pr: bool` to config-resolve command responses.
3. **Update GUI (`ui/`)**: Add checkbox to New Session form, wire to Tauri invoke, persist in `new_session_draft.json`.
4. **Update documentation**:
   - `cruise-schema.json`: add `cleanup_after_pr` top-level property.
   - `skills/cruise-config/references/top-level.md`: add sample yaml line and new section.
   - `skills/cruise-config/SKILL.md`: update authoring checklist link description.
   - `skills/cruise-config/references/after-pr.md`: add cross-reference.
   - `README.md`: add sample yaml line, new section, and CLI options.
5. **Run full quality gates**: `cargo test --workspace`, `cargo clippy --all-targets -- -D warnings`.

## Next-Agent Starting Position
1. Read the integration-test patterns at the bottom of `src/run_cmd.rs` (`make_current_branch_session`, `single_command_config`, `install_logging_gh`).
2. Add the six cleanup integration tests before the closing brace of the `#[cfg(test)]` module.
3. Proceed to `src-tauri/src/commands.rs`, then `ui/`, then docs in that order.
4. Finish with `cargo clippy --all-targets -- -D warnings`.

## Committed State
WIP commit: TODO — "fix cleanup_after_pr compile errors and partial move in run_cmd"
