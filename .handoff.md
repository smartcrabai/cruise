# Incomplete Handoff: after-pr skip steps support

## Status
Core implementation wired; pending cargo check/test verification and UI test mock updates.

## Done
- `src/workflow.rs`: `list_skippable_after_pr_steps` and tests already present.
- `src/worktree_pr.rs`: Added `skipped_steps: &[String]` to `handle_worktree_pr` and `run_after_pr_steps`; propagated into `ExecutionContext`.
- `src/run_cmd.rs`: Pass `&skipped_steps` to `handle_worktree_pr`.
- `src/plan_cmd.rs`: `select_steps_to_skip` now combines main and after-pr nodes; after-pr labels prefixed with `[after-pr] `.
- `src/list_cmd.rs`: `edit_session_settings_interactive` now uses expanded step IDs from both phases; after-pr labelled, current_step restricted to main-phase IDs.
- `src-tauri/src/commands.rs`: Added `after_pr_steps` to `NewSessionConfigDefaultsDto` and populated it via `list_skippable_after_pr_steps`.
- UI: Updated `NewSessionConfigDefaults` type, `SessionConfigEditor`, and `App` New Session form to fetch/render an "After PR Steps" section.
- Committed as `96e6f2b` on branch `cruise/20260623100151785_1a9d22cb65234c0f82ce53589928bf67-after-pr-skip-steps`.

## Remaining
1. **Verify Rust compilation/tests**: run `cargo check --lib`, `cargo test --workspace`. Fix any compile errors (e.g. import paths, borrow issues).
2. **Add/adjust Rust tests if needed**:
   - `src/engine.rs` already has `test_user_skipped_after_pr_step_is_not_executed`; verify it passes.
   - Consider adding a plan_cmd unit test for after-pr steps appearing in `select_steps_to_skip` (use a non-interactive harness or inspect returned `StepSkipSelection` directly).
3. **Update UI test mocks**: every `getNewSessionConfigDefaults` mock in `ui/src/**/*.test.tsx` must include `afterPrSteps: []` to satisfy the new TypeScript type. Add `afterPrSteps: []` next to `steps: ...` in all mocked responses.
4. **Run UI tests**: `npm test` (or `vitest`) to confirm no type/test regressions.
5. **Run quality gates**: `cargo clippy --all-targets -- -D warnings`.

## Next-Agent Starting Position
1. Run `cargo check --lib` and fix the first compile error that appears.
2. Once Rust compiles, run `cargo test --lib` focusing on workflow/engine/worktree_pr tests.
3. Update all `ui/src/**/*.test.tsx` mocks for `getNewSessionConfigDefaults` with `afterPrSteps: []`.
4. Run UI tests, then `cargo clippy --all-targets -- -D warnings`.
5. If everything passes, remove this handoff file and open a final summary PR message.

## Committed State
WIP commit: `96e6f2b` — "WIP: after-pr skip steps support"
